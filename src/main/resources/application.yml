spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    #    url: jdbc:mysql://${EC2_HOST}:3306/tripletDB?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    #    username: ${MYSQL_USERNAME}
    #    password: ${MYSQL_PASSWORD}
    url: jdbc:mysql://localhost:3306/squading?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: takealook
    password: '0205'

  jpa:
    #open-in-view: true
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
      #show-sql: true

logging:
  level:
    org:
      springframework:
        orm:
          jpa: DEBUG